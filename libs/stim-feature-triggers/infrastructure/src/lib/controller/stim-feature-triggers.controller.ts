import { Controller } from '@nestjs/common';

// import { StimFeatureTriggersFacade } from '../service/stim-feature-triggers.facade';

@Controller()
export class StimFeatureTriggersController {
  // private readonly logger = new Logger(StimFeatureTriggersController.name);
  //
  // constructor(private readonly facade: StimFeatureTriggersFacade) {}
}
